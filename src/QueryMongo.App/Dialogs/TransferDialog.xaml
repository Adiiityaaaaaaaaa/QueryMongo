<ContentDialog
    x:Class="QueryMongo.App.Dialogs.TransferDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Export collection"
    PrimaryButtonText="Export"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <StackPanel Spacing="12" Width="480">

        <TextBlock x:Name="DescriptionText" TextWrapping="Wrap" FontSize="12"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathBox" Grid.Column="0" Header="File" />
            <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse" VerticalAlignment="Bottom"
                    Click="OnBrowse" />
        </Grid>

        <StackPanel Spacing="6">
            <TextBlock Text="Format" FontSize="12" FontWeight="SemiBold" />
            <RadioButtons x:Name="FormatButtons" SelectedIndex="0">
                <x:String>JSON</x:String>
                <x:String>CSV</x:String>
            </RadioButtons>
        </StackPanel>

        <CheckBox x:Name="ExportAllCheck" Content="Export all matching documents (ignore the limit)"
                  IsChecked="True" />

        <InfoBar x:Name="ErrorBar" Severity="Error" IsClosable="False" IsOpen="False" />
    </StackPanel>
</ContentDialog>
